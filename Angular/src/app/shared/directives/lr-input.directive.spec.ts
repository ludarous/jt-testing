import { LrInputDirective } from './lr-input.directive';

describe('LrInputDirective', () => {
  it('should create an instance', () => {
    const directive = new LrInputDirective();
    expect(directive).toBeTruthy();
  });
});
